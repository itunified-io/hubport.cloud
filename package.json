{
  "name": "@itunified-io/hubport.cloud",
  "private": true,
  "version": "2026.04.04.22",
  "license": "MIT",
  "workspaces": [
    "central-api",
    "hub-app",
    "hub-api",
    "setup-wizard"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "dev:api": "npm run dev --workspace=hub-api",
    "dev:app": "npm run dev --workspace=hub-app",
    "dev:wizard": "npm run dev --workspace=setup-wizard",
    "dev:central": "npm run dev --workspace=central-api"
  }
}
